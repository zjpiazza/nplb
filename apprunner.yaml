version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install
run:
  runtime-version: 3.11.0
  command: pipenv run uvicorn nplb:app --host 0.0.0.0 --port 8080
  network:
    port: 8080