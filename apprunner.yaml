version: 1.0
runtime: python3
build:
  commands:
    - pip install -r requirements.txt  # Use a requirements.txt file instead of Pipenv
run:
  runtime-version: 3.11.0
  command: python -m uvicorn nplb:app --host 0.0.0.0 --port 8080
  network:
    port: 8080